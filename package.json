{
  "name": "chrono-mcp",
  "version": "0.1.0",
  "description": "MCP server for parsing natural language date/time expressions using chrono-node, deployable on Cloudflare Workers",
  "type": "module",
  "private": true,
  "author": "upamune",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/upamune/chrono-mcp.git"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "chrono",
    "date-parser",
    "natural-language",
    "cloudflare-workers",
    "timezone"
  ],
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "bunx tsc -p tsconfig.worker.json --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "chrono-node": "2.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/bun": "latest",
    "typescript": "^5.7.2",
    "wrangler": "^3.94.0"
  }
}
